<tabs>
    <tab
        id="@+id/tab_home"
        icon="@mipmap/ic_home"
        title="Home"
        />
    <tab
        id="@+id/tab_menu"
        icon="@mipmap/ic_view_list"
        title="Menu" />
    <tab
        id="@+id/tab_personal"
        icon="@mipmap/ic_face"
        title="Personal" />
    <tab
        id="@+id/tab_search"
        icon="@mipmap/ic_search"
        title="Search" />
</tabs>